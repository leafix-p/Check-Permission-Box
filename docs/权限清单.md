# 权限清单

---

## 特殊权限

### MANAGE_EXTERNAL_STORAGE

- **权限字符串值：** android.permission.MANAGE_EXTERNAL_STORAGE
- **权限用途：** 允许应用访问设备上的所有文件（外置存储全部文件读写权限），无需用户逐个文件夹授权。
- **适用系统 API 范围：** Android 11 (API 30) 及以上
- **权限申请方法：**
  1. 在 AndroidManifest.xml 中声明权限：
     `<uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />`
  2. 在代码中检查权限是否已授予：
     ```kotlin
     if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.R) {
         Environment.isExternalStorageManager()
     }
     ```
  3. 若未授权，跳转系统设置页面引导用户授权：
     ```kotlin
     val intent = Intent(Settings.ACTION_MANAGE_APP_ALL_FILES_ACCESS_PERMISSION).apply {
         data = Uri.parse("package:${context.packageName}")
     }
     startActivity(intent)
     ```
  4. 使用 `ActivityResultLauncher` 或监听 `onResume` 生命周期，在用户返回后重新检查权限状态。

---

## 媒体与传感器

### CAMERA

- **权限字符串值：** android.permission.CAMERA
- **权限用途：** 允许应用拍摄照片和视频
- **适用系统 API 范围：** 所有版本
- **权限申请方法：** 运行时权限，通过 `ActivityResultContracts.RequestPermission()` 弹出系统对话框申请

### RECORD_AUDIO

- **权限字符串值：** android.permission.RECORD_AUDIO
- **权限用途：** 允许应用录制音频
- **适用系统 API 范围：** 所有版本
- **权限申请方法：** 运行时权限，通过 `ActivityResultContracts.RequestPermission()` 弹出系统对话框申请

### READ_MEDIA_IMAGES

- **权限字符串值：** android.permission.READ_MEDIA_IMAGES
- **权限用途：** 允许应用读取设备中的图片文件（Android 13 替代 READ_EXTERNAL_STORAGE）
- **适用系统 API 范围：** Android 13 (API 33) 及以上
- **权限申请方法：** 运行时权限，通过 `ActivityResultContracts.RequestPermission()` 弹出系统对话框申请

### READ_MEDIA_VIDEO

- **权限字符串值：** android.permission.READ_MEDIA_VIDEO
- **权限用途：** 允许应用读取设备中的视频文件
- **适用系统 API 范围：** Android 13 (API 33) 及以上
- **权限申请方法：** 运行时权限，通过 `ActivityResultContracts.RequestPermission()` 弹出系统对话框申请

### READ_MEDIA_AUDIO

- **权限字符串值：** android.permission.READ_MEDIA_AUDIO
- **权限用途：** 允许应用读取设备中的音频文件
- **适用系统 API 范围：** Android 13 (API 33) 及以上
- **权限申请方法：** 运行时权限，通过 `ActivityResultContracts.RequestPermission()` 弹出系统对话框申请

---

## 位置

### ACCESS_FINE_LOCATION

- **权限字符串值：** android.permission.ACCESS_FINE_LOCATION
- **权限用途：** 允许应用获取设备的精确定位
- **适用系统 API 范围：** 所有版本
- **权限申请方法：** 运行时权限，通过 `ActivityResultContracts.RequestPermission()` 弹出系统对话框申请

### ACCESS_COARSE_LOCATION

- **权限字符串值：** android.permission.ACCESS_COARSE_LOCATION
- **权限用途：** 允许应用获取设备的粗略位置
- **适用系统 API 范围：** 所有版本
- **权限申请方法：** 运行时权限，通过 `ActivityResultContracts.RequestPermission()` 弹出系统对话框申请

### ACCESS_BACKGROUND_LOCATION

- **权限字符串值：** android.permission.ACCESS_BACKGROUND_LOCATION
- **权限用途：** 允许应用在后台访问位置信息
- **适用系统 API 范围：** Android 10 (API 29) 及以上
- **权限申请方法：** 运行时权限，通过 `ActivityResultContracts.RequestPermission()` 弹出系统对话框申请

---

## 通讯与数据

### READ_CONTACTS

- **权限字符串值：** android.permission.READ_CONTACTS
- **权限用途：** 允许应用读取设备中的联系人
- **适用系统 API 范围：** 所有版本
- **权限申请方法：** 运行时权限，通过 `ActivityResultContracts.RequestPermission()` 弹出系统对话框申请

### READ_CALENDAR

- **权限字符串值：** android.permission.READ_CALENDAR
- **权限用途：** 允许应用读取设备中的日历事件
- **适用系统 API 范围：** 所有版本
- **权限申请方法：** 运行时权限，通过 `ActivityResultContracts.RequestPermission()` 弹出系统对话框申请

### READ_SMS

- **权限字符串值：** android.permission.READ_SMS
- **权限用途：** 允许应用读取设备中的短信
- **适用系统 API 范围：** 所有版本
- **权限申请方法：** 运行时权限，通过 `ActivityResultContracts.RequestPermission()` 弹出系统对话框申请

### READ_PHONE_STATE

- **权限字符串值：** android.permission.READ_PHONE_STATE
- **权限用途：** 允许应用读取设备状态和身份信息
- **适用系统 API 范围：** 所有版本
- **权限申请方法：** 运行时权限，通过 `ActivityResultContracts.RequestPermission()` 弹出系统对话框申请

### BODY_SENSORS

- **权限字符串值：** android.permission.BODY_SENSORS
- **权限用途：** 允许应用访问身体传感器数据
- **适用系统 API 范围：** 所有版本
- **权限申请方法：** 运行时权限，通过 `ActivityResultContracts.RequestPermission()` 弹出系统对话框申请

---

## 通知

### POST_NOTIFICATIONS

- **权限字符串值：** android.permission.POST_NOTIFICATIONS
- **权限用途：** 允许应用发送通知（Android 13+ 系统对话框授权）
- **适用系统 API 范围：** Android 13 (API 33) 及以上
- **权限申请方法：** 运行时权限，通过 `ActivityResultContracts.RequestPermission()` 弹出系统对话框申请

### NOTIFICATION_SETTINGS

- **权限字符串值：** （无权限声明，系统设置页面管理）
- **权限用途：** 跳转系统通知设置页面管理（兼容所有版本）
- **适用系统 API 范围：** 所有版本
- **权限申请方法：** 通过 `Settings.ACTION_APP_NOTIFICATION_SETTINGS` 跳转系统通知设置页面

---

## 蓝牙

### BLUETOOTH_CONNECT

- **权限字符串值：** android.permission.BLUETOOTH_CONNECT
- **权限用途：** 允许应用连接和控制蓝牙设备
- **适用系统 API 范围：** Android 12 (API 31) 及以上
- **权限申请方法：** 运行时权限，通过 `ActivityResultContracts.RequestPermission()` 弹出系统对话框申请

### BLUETOOTH_SCAN

- **权限字符串值：** android.permission.BLUETOOTH_SCAN
- **权限用途：** 允许应用扫描附近的蓝牙设备
- **适用系统 API 范围：** Android 12 (API 31) 及以上
- **权限申请方法：** 运行时权限，通过 `ActivityResultContracts.RequestPermission()` 弹出系统对话框申请

---

## 注意事项

- MANAGE_EXTERNAL_STORAGE 在 Google Play 商店有严格限制，仅限文件管理器、备份、杀毒等特定类别应用使用。
- POST_NOTIFICATIONS、READ_MEDIA_IMAGES 为 Android 13 新增运行时权限，低版本系统会自动跳过。
- BLUETOOTH_CONNECT 为 Android 12 新增运行时权限，低版本系统会自动跳过。
- 本示例仅用于学习和展示 Android 权限申请机制。
